#include<bits/stdc++.h>
using namespace std;
int main(){
	int num;
	cin>>num;
	for(int i=0;i<num;i++){
		string str1;
		str1.clear();
		cin>>str1;
		
		if(str1.size()>10){
			cout<<*str1.begin()<<(str1.size()-2)<<*str1.rbegin()<<"\n";
		}
		else{
			cout<<str1<<"\n";
		}
	}
	return 0;
	
}
